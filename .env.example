# Copy to .env.local for local dev. Add the same keys in Vercel > Project >
# Settings > Environment Variables. These are client-side Firebase keys (safe to
# expose) — your real protection is Firestore Security Rules, not secrecy.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# --- Calls: TURN relay (optional) ------------------------------------------
# STUN (free, public) is always on. TURN rescues cross-network calls that STUN
# can't connect (mobile data / symmetric NAT). Leave blank to run STUN-only.
# Get ready-to-use credentials from metered.ca, Twilio NTS, or self-host coturn.
VITE_TURN_URL=
VITE_TURN_USERNAME=
VITE_TURN_CREDENTIAL=
